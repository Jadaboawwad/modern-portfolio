# Portfolio chat — browser calls ngrok directly for /api/chat (avoids Netlify ~26s timeout).
# Streamlit embed still uses /chat edge proxy.
NEXT_PUBLIC_CHATBOT_URL=https://jehadabuawwad.com/chat
NEXT_PUBLIC_RAG_API_URL=https://curable-steerable-obnoxious.ngrok-free.dev
