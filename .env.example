# Chat iframe uses the Netlify edge proxy (not ngrok directly).
# Remove any ngrok URL from Netlify UI env vars if the live bundle still embeds ngrok.
NEXT_PUBLIC_CHATBOT_URL=https://jehadabuawwad.com/chat
